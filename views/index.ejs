

<!-- Hero Section -->
<section class="hero-section bg-primary text-white py-5">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-6">
                <h1 class="display-4 fw-bold mb-4">Find & Book Appointments with Top Doctors</h1>
                <p class="lead mb-4">Connect with qualified healthcare professionals and book appointments online. Your health is our priority.</p>
                
                <!-- Search Form -->
                <div class="card bg-white p-4 shadow">
                    <form action="/search" method="GET">
                        <div class="row g-3">
                            <div class="col-md-8">
                                <input type="text" class="form-control form-control-lg" name="query" placeholder="Search by doctor name, specialization, or department...">
                            </div>
                            <div class="col-md-4">
                                <select class="form-select form-select-lg" name="department">
                                    <option value="all">All Departments</option>
                                    <% departments.forEach(dept => { %>
                                        <option value="<%= dept %>"><%= dept %></option>
                                    <% }) %>
                                </select>
                            </div>
                            <div class="col-12">
                                <button type="submit" class="btn btn-primary btn-lg w-100">
                                    <i class="fas fa-search me-2"></i>Find Doctors
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
            <div class="col-lg-6">
                <img src="/images/Doctor.jpg" alt="Doctor" class="img-fluid rounded shadow" style="max-height: 400px; object-fit: cover;">
            </div>
        </div>
    </div>
</section>

<!-- Services Section -->
<section class="py-5">
    <div class="container">
        <div class="text-center mb-5">
            <h2 class="fw-bold">Our Services</h2>
            <p class="text-muted">Comprehensive healthcare services at your fingertips</p>
        </div>
        
        <div class="row g-4">
            <div class="col-md-4">
                <div class="card h-100 text-center border-0 shadow-sm">
                    <div class="card-body p-4">
                        <div class="bg-primary text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 60px; height: 60px;">
                            <i class="fas fa-calendar-check fa-lg"></i>
                        </div>
                        <h5 class="fw-bold">Easy Booking</h5>
                        <p class="text-muted">Book appointments online with your preferred doctors in just a few clicks.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card h-100 text-center border-0 shadow-sm">
                    <div class="card-body p-4">
                        <div class="bg-success text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 60px; height: 60px;">
                            <i class="fas fa-user-md fa-lg"></i>
                        </div>
                        <h5 class="fw-bold">Qualified Doctors</h5>
                        <p class="text-muted">Access to experienced and certified healthcare professionals across all specialties.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card h-100 text-center border-0 shadow-sm">
                    <div class="card-body p-4">
                        <div class="bg-info text-white rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 60px; height: 60px;">
                            <i class="fas fa-clock fa-lg"></i>
                        </div>
                        <h5 class="fw-bold">Flexible Timings</h5>
                        <p class="text-muted">Choose appointment slots that fit your schedule with flexible timing options.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Featured Doctors -->
<% if (doctors.length > 0) { %>
<section class="py-5 bg-light">
    <div class="container">
        <div class="text-center mb-5">
            <h2 class="fw-bold">Featured Doctors</h2>
            <p class="text-muted">Meet our expert healthcare professionals</p>
        </div>
        
        <div class="row g-4">
            <% doctors.slice(0, 6).forEach(doctor => { %>
                <div class="col-lg-4 col-md-6">
                    <div class="card h-100 border-0 shadow-sm">
                        <div class="card-body p-4 text-center">
                            <img src="<%= doctor.profileImage %>" alt="Dr. <%= doctor.name %>" class="rounded-circle mb-3" style="width: 80px; height: 80px; object-fit: cover;">
                            <h5 class="fw-bold">Dr. <%= doctor.name %></h5>
                            <p class="text-primary mb-2"><%= doctor.specialization %></p>
                            <p class="text-muted small mb-3"><%= doctor.department %></p>
                            <p class="text-muted small mb-3"><%= doctor.experience %> years experience</p>
                            <p class="fw-bold text-success mb-3">₹<%= doctor.consultationFee %></p>
                            <div class="d-grid gap-2">
                                <a href="/doctor/<%= doctor._id %>" class="btn btn-outline-primary">View Profile</a>
                                <% if (locals.user) { %>
                                    <a href="/appointments/book/<%= doctor._id %>" class="btn btn-primary">Book Appointment</a>
                                <% } else { %>
                                    <a href="/auth/login" class="btn btn-primary">Login to Book</a>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
        
        <div class="text-center mt-4">
            <a href="/search" class="btn btn-primary btn-lg">View All Doctors</a>
        </div>
    </div>
</section>
<% } %>

<!-- Statistics -->
<section class="py-5">
    <div class="container">
        <div class="row text-center g-4">
            <div class="col-md-3">
                <div class="p-4">
                    <h2 class="display-4 fw-bold text-primary">500+</h2>
                    <p class="text-muted">Qualified Doctors</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="p-4">
                    <h2 class="display-4 fw-bold text-success">10K+</h2>
                    <p class="text-muted">Happy Patients</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="p-4">
                    <h2 class="display-4 fw-bold text-info">50+</h2>
                    <p class="text-muted">Specialties</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="p-4">
                    <h2 class="display-4 fw-bold text-warning">24/7</h2>
                    <p class="text-muted">Support</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="py-5 bg-primary text-white">
    <div class="container text-center">
        <h2 class="fw-bold mb-4">Ready to Get Started?</h2>
        <p class="lead mb-4">Join thousands of patients who trust us with their healthcare needs.</p>
        <% if (!locals.user) { %>
            <div class="d-inline-flex gap-3">
                <a href="/auth/register" class="btn btn-light btn-lg">Register Now</a>
                <a href="/auth/login" class="btn btn-outline-light btn-lg">Login</a>
            </div>
        <% } else { %>
            <a href="/search" class="btn btn-light btn-lg">Find a Doctor</a>
        <% } %>
    </div>
</section>